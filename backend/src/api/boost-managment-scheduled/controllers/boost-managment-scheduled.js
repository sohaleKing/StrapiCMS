'use strict';

/**
 *  boost-managment-scheduled controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::boost-managment-scheduled.boost-managment-scheduled');
