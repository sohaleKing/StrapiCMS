'use strict';

/**
 * boost-managment-scheduled router.
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::boost-managment-scheduled.boost-managment-scheduled');
